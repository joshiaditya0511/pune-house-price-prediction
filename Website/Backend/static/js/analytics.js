document.addEventListener('DOMContentLoaded', function() {
    const analyticsNav = document.getElementById('sidebar-analytics');
    if (analyticsNav) {
      // Add a class that styles the item as "selected"
      analyticsNav.classList.add('selected');
    }
  });