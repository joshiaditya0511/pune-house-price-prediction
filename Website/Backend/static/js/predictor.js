// Example to highlight the 'Predictor' item in the sidebar
document.addEventListener('DOMContentLoaded', function() {
    const predictorNav = document.getElementById('sidebar-predictor');
    if (predictorNav) {
      // Add a class that styles the item as "selected"
      predictorNav.classList.add('selected');
    }
  });